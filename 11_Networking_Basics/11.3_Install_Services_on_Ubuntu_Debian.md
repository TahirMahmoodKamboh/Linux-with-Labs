# Installing MySQL, PostgreSQL, Redis, and Nginx on Ubuntu/Debian via Scripts

This guide provides shell scripts to install common services on Ubuntu/Debian systems.

---

## MySQL
```bash
#!/bin/bash
sudo apt update
sudo apt install -y mysql-server
sudo systemctl enable mysql
sudo systemctl start mysql
```

## PostgreSQL
```bash
#!/bin/bash
sudo apt update
sudo apt install -y postgresql postgresql-contrib
sudo systemctl enable postgresql
sudo systemctl start postgresql
```

## Redis
```bash
#!/bin/bash
sudo apt update
sudo apt install -y redis-server
sudo systemctl enable redis-server
sudo systemctl start redis-server
```

## Nginx
```bash
#!/bin/bash
sudo apt update
sudo apt install -y nginx
sudo systemctl enable nginx
sudo systemctl start nginx
```

---

## Lab
1. Write a script to install and start MySQL on Ubuntu.
2. Write a script to install and start Nginx on Ubuntu.

---

## Solutions

### MySQL
See script above under MySQL section.

### Nginx
See script above under Nginx section.